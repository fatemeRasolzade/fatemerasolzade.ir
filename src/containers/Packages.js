import TransitionText from "../components/TransitionText";

const Packages = () => {
  return (
    <div className="section flex justify-center items-center">
      <TransitionText text="Packages & Libraries" wrapperClassName="mt-4" />
    </div>
  );
};

export default Packages;
